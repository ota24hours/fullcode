import 'package:flutter/material.dart';

var primaryColor = const Color.fromRGBO(255, 255, 255, 1.0);

    const primaryOrange = Color.fromRGBO(207, 37, 40, 1);
    const darkText = Color(0xFF2E1D1D);
    const greyText = Color(0xFF8D8D8D);
